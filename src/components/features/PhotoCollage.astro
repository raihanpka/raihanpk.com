---
/**
 * Aesthetic Photo Collage component for Core Memories with friends.
 * Follows clean uniform aspect ratios, enlarged 2-line wrapped captions,
 * no AI-slop dots, and auto marquee if photos > 3.
 */

interface PhotoItem {
  src: string
  caption: string
}

interface Props {
  photos?: PhotoItem[]
}

const defaultPhotos: PhotoItem[] = [
  {
    src: '/static/twitter-card.png',
    caption: 'Late night sprints and caffeinated ideas with the crew',
  },
  {
    src: '/static/kalanusa.png',
    caption: 'Podium celebrations and the relief of shipping together',
  },
  {
    src: '/static/satria.png',
    caption: 'Between deadlines: quiet laughter that outlasts the code',
  },
]

const { photos = defaultPhotos } = Astro.props

// Always marquee on mobile (< sm). On desktop (sm+), marquee if > 3 photos.
const isAlwaysMarquee = photos.length > 3
const marqueeItems = photos.length <= 3
  ? [...photos, ...photos, ...photos, ...photos]
  : [...photos, ...photos]
---

<section class="mt-2 pt-4 border-t border-border/50">
  <div class="mb-5">
    <h2 class="text-xl font-semibold text-foreground">
      Core Memories
    </h2>
    <p class="text-sm text-muted-foreground mt-1">
      Fragments of time, shared laughter, and quiet moments with friends along the journey.
    </p>
  </div>

  {/* Mobile Marquee (always active on mobile < sm, and everywhere if > 3 photos) */}
  <div class={`group relative -mx-4 sm:-mx-6 px-4 sm:px-6 overflow-hidden py-2 ${isAlwaysMarquee ? 'block' : 'block sm:hidden'}`}>
    {/* Gradient edge fades */}
    <div class="pointer-events-none absolute inset-y-0 left-0 z-10 w-10 sm:w-12 bg-gradient-to-r from-background to-transparent" />
    <div class="pointer-events-none absolute inset-y-0 right-0 z-10 w-10 sm:w-12 bg-gradient-to-l from-background to-transparent" />

    <div class="animate-core-marquee gap-4 sm:gap-5">
      {marqueeItems.map((item) => (
        <figure class="w-64 sm:w-80 shrink-0 flex flex-col overflow-hidden rounded-xl border border-border/80 bg-card/60 p-3 sm:p-3.5 shadow-sm transition-all duration-300 hover:border-foreground/40 hover:-translate-y-1 hover:shadow-md">
          {/* Uniform Aspect Ratio Photo */}
          <div class="relative w-full aspect-[4/3] overflow-hidden rounded-lg bg-secondary/50 border border-border/40">
            <img
              src={item.src}
              alt={item.caption}
              loading="lazy"
              decoding="async"
              class="h-full w-full object-cover grayscale contrast-110 transition-all duration-500 ease-out hover:grayscale-0"
            />
            <div class="pointer-events-none absolute inset-0 bg-gradient-to-t from-black/25 via-transparent to-transparent opacity-40" />
          </div>

          {/* Enlarged 2-Line Caption matching width */}
          <figcaption class="mt-2.5 sm:mt-3 w-full">
            <p class="text-sm sm:text-base font-medium text-foreground/90 leading-snug line-clamp-2">
              {item.caption}
            </p>
          </figcaption>
        </figure>
      ))}
    </div>
  </div>

  {/* Desktop Grid (rendered only when <= 3 photos on screens >= sm) */}
  {!isAlwaysMarquee && (
    <div class="hidden sm:grid sm:grid-cols-2 lg:grid-cols-3 gap-5">
      {photos.map((item) => (
        <figure class="group flex flex-col overflow-hidden rounded-xl border border-border/80 bg-card/60 p-3.5 shadow-sm transition-all duration-300 hover:border-foreground/40 hover:-translate-y-1 hover:shadow-md">
          {/* Uniform Aspect Ratio Photo */}
          <div class="relative w-full aspect-[4/3] overflow-hidden rounded-lg bg-secondary/50 border border-border/40">
            <img
              src={item.src}
              alt={item.caption}
              loading="lazy"
              decoding="async"
              class="h-full w-full object-cover grayscale contrast-110 transition-all duration-500 ease-out group-hover:grayscale-0 group-hover:scale-105"
            />
            <div class="pointer-events-none absolute inset-0 bg-gradient-to-t from-black/25 via-transparent to-transparent opacity-40 group-hover:opacity-10 transition-opacity duration-300" />
          </div>

          {/* Enlarged 2-Line Caption matching width */}
          <figcaption class="mt-3 w-full">
            <p class="text-sm sm:text-base font-medium text-foreground/90 leading-snug line-clamp-2">
              {item.caption}
            </p>
          </figcaption>
        </figure>
      ))}
    </div>
  )}
</section>

<style>
  @keyframes core-memories-marquee {
    0% {
      transform: translate3d(0, 0, 0);
    }
    100% {
      transform: translate3d(-50%, 0, 0);
    }
  }

  .animate-core-marquee {
    display: flex;
    width: max-content;
    will-change: transform;
    animation: core-memories-marquee 30s linear infinite;
  }

  .animate-core-marquee:hover,
  .group:hover .animate-core-marquee {
    animation-play-state: paused;
  }
</style>
